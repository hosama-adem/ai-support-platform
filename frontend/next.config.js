/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // We'll add more config as needed, but for now keep it minimal
}

module.exports = nextConfig
